package org.hammer.colombo.utils;

import java.util.ArrayList;

public class Space {

	/**
	 * cos(angle(v(s), v(q)).
	 * 
	 * Suppose that the main query "q" has [1,1,1,1,1,1,...]
	 * Calc cos between q and q' : it is the similarity!
	 */
	public static double cos(ArrayList<Term[]> qtest) {
		
		return 0.0d;
	}
}
